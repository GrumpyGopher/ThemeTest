name: muOS Complete Archive Manager Theme Releases

on:
  push:
    branches: ["main"]
  workflow_dispatch:
    
concurrency: 
  group: release

jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      contents: write
      pull-requests: write
      repository-projects: write
    steps:
      - name: Checkout repo
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
# Create theme archives here! Zipping everything together and creating a complete theme archive.
      - name: Zip Legacy directories
        run: |
          mkdir -p /tmp/release
          for dir in current/*/; do
            [ -d "$dir" ] || continue
            zip_name=$(basename "$dir")
            zip_path="/tmp/release/${zip_name}.muxthm"
            echo "Zipping contents of $dir into $zip_path"
            
            (cd "$dir" && zip -qq -r "$zip_path" ./*)
          done
          
# Create the archive!          
      - name: List archives
        run: |
          ls -1 /tmp/release
          
      - name: Delete old releases (older than 3 months)
        run: |
          # Get latest release tag (never delete this one)
          LATEST=$(gh release view --json tagName -q '.tagName')
          echo "Latest release: $LATEST"

          # Get releases older than 90 days
          OLD_RELEASES=$(gh release list --limit 1000 --json name,tagName,createdAt -q \
            '.[] | select((.createdAt | fromdateiso8601) < (now - (90*24*60*60))) | {tag: .tagName, name: .name}')

          if [[ -n "$OLD_RELEASES" ]]; then
            echo "$OLD_RELEASES" | while read -r release; do
              tag=$(echo "$release" | jq -r '.tag')
              name=$(echo "$release" | jq -r '.name')

              # Skip if it's the latest release
              if [[ "$tag" == "$LATEST" ]]; then
                echo "Skipping latest release: $tag  $name"
                continue
              fi

              # Skip if last Jacaranda release
              if [[ "$name" == "Jacaranda Themes Final Release" ]]; then
                echo "Name Match, Skipping last Jacaranda release: $tag  $name"
                continue
              fi
              
              # Skip if last Goose release
              if [[ "$name" == "Goose Themes Final Release" ]]; then
                echo "Name Match, Skipping last Goose release: $tag  $name"
                continue
              fi

              # Skip if last Pixie release
              if [[ "$name" == "Pixie Themes Final Release" ]]; then
                echo "Name Match, Skipping last Pixie release: $tag  $name"
                continue
              fi

              # Skip if it's the latest release
              if [[ "$tag" == "2025-08-23_1523" ]]; then
                echo "Tag Match, Skipping last Pixie release: $tag  $name"
                continue
              fi

              # Otherwise delete it
              echo "Deleting release: $tag  $name"
              gh release delete "$tag" -y
            done
          else
            echo "No releases older than cutoff found"
          fi
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Get release name for artifacts
        id: date
        run: |
            echo "date=$(date +'%Y-%m-%d_%H%M')" >> $GITHUB_OUTPUT

      - name: Prepare Release
        uses: ncipollo/release-action@v1.14.0
        with:
          name: "2606.0 Andromeda Themes ${{ steps.date.outputs.date }}"
          tag: "2606.0_Andromeda_${{ steps.date.outputs.date }}"
          allowUpdates: true
          draft: true
          prerelease: false
          replacesArtifacts: false
          omitNameDuringUpdate: true
          artifacts: "/tmp/release/*.muxthm"
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: Publish Release
        uses: ncipollo/release-action@v1.14.0
        with:
          name: "2606.0 Andromeda Themes ${{ steps.date.outputs.date }}"
          tag: "2606.0_Andromeda_${{ steps.date.outputs.date }}"
          omitBodyDuringUpdate: true
          omitNameDuringUpdate: true
          allowUpdates: true
          draft: false
          prerelease: false
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: Update Static Track Release (No Tag Movement)
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          STATIC_TAG="2606.0_Andromeda"

          # 1. If the static release page doesn't exist yet, create it once
          if ! gh release view "$STATIC_TAG" >/dev/null 2>&1; then
            gh release create "$STATIC_TAG" \
              --title "Latest 2606.0 Andromeda Build"
          fi

          # 2. Clear out the old zip files from the static release page
          echo "Clearing old assets..."
          for asset in $(gh release view "$STATIC_TAG" --json assets --jq '.assets[].name'); do
            gh release delete-asset "$STATIC_TAG" "$asset" -y || true
          done

          # 3. Upload the fresh zip files to the static release page
          echo "Uploading new assets..."
          gh release upload "$STATIC_TAG" /tmp/release/*.muxthm --clobber

          # 4. Update the text to point to the newest commit/history tag
          gh release edit "$STATIC_TAG" \
            --notes "Static URL tracking track. Current build artifact is synced from history release: 2606.0_${{ steps.date.outputs.date }}"

      - name: Release Info
        id: info
        run: |
          echo "Published themes: ${{steps.date.outputs.date}} to: https://github.com/${{ steps.env.outputs.RELEASE_ORG}}/${{ steps.env.outputs.RELEASE_REPO}}"
          
